# Creating databases {#sec-change-db}


```{r}
#| include: false

source("_common.R")
```


from mdsr changing data (update) and adding data (insert, insert ignore, replace)


also, go back to some of the select queries, do complicated things, save as a new table, then join.
